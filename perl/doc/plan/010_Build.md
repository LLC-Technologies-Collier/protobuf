# Milestone 1: Build System & C Test Framework

[TOC]

*   [x] REFRESH: Review all documents in @perl/doc/guidelines/**
*   [x] Setup build system (MakeMaker) for Perl & XS.
*   [x] Configure `MY::postamble` for building `libprotobuf_common.$(DLEXT)` (built as shared library instead of .a).
*   [x] Configure `MY::postamble` for compiling C tests in `t/c/` (including subdirectories).
*   [x] Implement C-level test harness (`t/c/upb-perl-test.h`).
*   [x] Review and update perl/doc/architecture/** documents based on this milestone's learnings.
