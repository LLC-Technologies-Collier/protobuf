# Milestone 1: Build System & C Test Framework

[TOC]

*   [ ] REFRESH: Review all documents in @perl/doc/guidelines/**
*   [ ] Setup build system (MakeMaker) for Perl & XS.
*   [ ] Configure `MY::postamble` for building `libprotobuf_common.a`.
*   [ ] Configure `MY::postamble` for compiling C tests in `t/c/` (including subdirectories).
*   [ ] Implement C-level test harness (`t/c/upb-perl-test.h`).
*   [ ] Review and update perl/doc/architecture/** documents based on this milestone's learnings.
