# Milestone 6: C Layer - descriptor

[TOC]

*   [x] REFRESH: Review all documents in @perl/doc/guidelines/**

*   **xs/descriptor/base.c:**
    *   [x] Create test file `t/c/descriptor/base.c`.
    *   [ ] Tests cover all public functions in the corresponding .h file, including checks for re-entrancy and safe state management. (TODO stub created).
    *   [x] Implement C functions.
    *   [ ] C tests pass.
*   **xs/descriptor/message.c:**
    *   [x] Create test file `t/c/descriptor/message.c`.
    *   [ ] Tests cover all public functions in the corresponding .h file, including checks for re-entrancy and safe state management. (TODO stub created).
    *   [x] Implement C functions.
    *   [ ] C tests pass.
*   **xs/descriptor/field.c:**
    *   [x] Create test file `t/c/descriptor/field.c`.
    *   [ ] Tests cover all public functions in the corresponding .h file, including checks for re-entrancy and safe state management. (TODO stub created).
    *   [x] Implement C functions.
    *   [ ] C tests pass.
*   **xs/descriptor/enum.c:**
    *   [x] Create test file `t/c/descriptor/enum.c`.
    *   [ ] Tests cover all public functions in the corresponding .h file, including checks for re-entrancy and safe state management. (TODO stub created).
    *   [x] Implement C functions.
    *   [ ] C tests pass.
*   **xs/descriptor/enum_value.c:**
    *   [x] Create test file `t/c/descriptor/enum_value.c`.
    *   [ ] Tests cover all public functions in the corresponding .h file, including checks for re-entrancy and safe state management. (TODO stub created).
    *   [x] Implement C functions.
    *   [ ] C tests pass.
*   **xs/descriptor/oneof.c:**
    *   [x] Create test file `t/c/descriptor/oneof.c`.
    *   [ ] Tests cover all public functions in the corresponding .h file, including checks for re-entrancy and safe state management. (TODO stub created).
    *   [x] Implement C functions.
    *   [ ] C tests pass.
*   **xs/descriptor/file.c:**
    *   [x] Create test file `t/c/descriptor/file.c`.
    *   [ ] Tests cover all public functions in the corresponding .h file, including checks for re-entrancy and safe state management. (TODO stub created).
    *   [x] Implement C functions.
    *   [ ] C tests pass.
*   **xs/descriptor/service.c:**
    *   [x] Create test file `t/c/descriptor/service.c`.
    *   [ ] Tests cover all public functions in the corresponding .h file, including checks for re-entrancy and safe state management. (TODO stub created).
    *   [x] Implement C functions.
    *   [ ] C tests pass.
*   **xs/descriptor/method.c:**
    *   [x] Create test file `t/c/descriptor/method.c`.
    *   [ ] Tests cover all public functions in the corresponding .h file, including checks for re-entrancy and safe state management. (TODO stub created).
    *   [x] Implement C functions.
    *   [ ] C tests pass.
*   [ ] Review and update perl/doc/architecture/** documents based on this milestone's learnings.
