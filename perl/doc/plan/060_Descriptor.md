# Milestone 6: C Layer - descriptor

[TOC]

*   [ ] REFRESH: Review all documents in @perl/doc/guidelines/**

*   **xs/descriptor/base.c:**
    *   [ ] Create test file `t/c/descriptor/base.c`.
    *   [ ] Tests cover all public functions in the corresponding .h file, including checks for re-entrancy and safe state management.
    *   [ ] Implement C functions.
    *   [ ] C tests pass.
*   **xs/descriptor/message.c:**
    *   [ ] Create test file `t/c/descriptor/message.c`.
    *   [ ] Tests cover all public functions in the corresponding .h file, including checks for re-entrancy and safe state management.
    *   [ ] Implement C functions.
    *   [ ] C tests pass.
*   **xs/descriptor/field.c:**
    *   [ ] Create test file `t/c/descriptor/field.c`.
    *   [ ] Tests cover all public functions in the corresponding .h file, including checks for re-entrancy and safe state management.
    *   [ ] Implement C functions.
    *   [ ] C tests pass.
*   **xs/descriptor/enum.c:**
    *   [ ] Create test file `t/c/descriptor/enum.c`.
    *   [ ] Tests cover all public functions in the corresponding .h file, including checks for re-entrancy and safe state management.
    *   [ ] Implement C functions.
    *   [ ] C tests pass.
*   **xs/descriptor/enum_value.c:**
    *   [ ] Create test file `t/c/descriptor/enum_value.c`.
    *   [ ] Tests cover all public functions in the corresponding .h file, including checks for re-entrancy and safe state management.
    *   [ ] Implement C functions.
    *   [ ] C tests pass.
*   **xs/descriptor/oneof.c:**
    *   [ ] Create test file `t/c/descriptor/oneof.c`.
    *   [ ] Tests cover all public functions in the corresponding .h file, including checks for re-entrancy and safe state management.
    *   [ ] Implement C functions.
    *   [ ] C tests pass.
*   **xs/descriptor/file.c:**
    *   [ ] Create test file `t/c/descriptor/file.c`.
    *   [ ] Tests cover all public functions in the corresponding .h file, including checks for re-entrancy and safe state management.
    *   [ ] Implement C functions.
    *   [ ] C tests pass.
*   **xs/descriptor/service.c:**
    *   [ ] Create test file `t/c/descriptor/service.c`.
    *   [ ] Tests cover all public functions in the corresponding .h file, including checks for re-entrancy and safe state management.
    *   [ ] Implement C functions.
    *   [ ] C tests pass.
*   **xs/descriptor/method.c:**
    *   [ ] Create test file `t/c/descriptor/method.c`.
    *   [ ] Tests cover all public functions in the corresponding .h file, including checks for re-entrancy and safe state management.
    *   [ ] Implement C functions.
    *   [ ] C tests pass.
*   [ ] Review and update perl/doc/architecture/** documents based on this milestone's learnings.
